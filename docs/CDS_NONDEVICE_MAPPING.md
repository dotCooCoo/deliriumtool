# Non-Device CDS design rubric — output-by-output mapping

**Purpose.** A design check, not legal advice. Each clinical *output* of the tool is held against the four FDA Non-Device Clinical Decision Support criteria (21st Century Cures Act §520(o)(1)(E), as structured in the Jan-2026 CDS final guidance). The goal is that every output is **built so it would qualify** as non-device CDS — even though an internal, non-distributed reference aid is outside FDA premarket reach.

This mapping describes the tool **as currently implemented**. Earlier revisions of this document recorded design gaps (weighted risk scores, an unvalidated Pictures-ASE cut-point, an uncited thiamine dose, mislabeled subtype "prevalence"); those gaps were remediated in the 2026-06-27 and 2026-07-01 content revisions and their resolutions are recorded in `CLINICAL_METHODOLOGY.md` §6.5. Where a row below says "resolved", the §6.5 change log carries the dated entry.

**The four criteria (as applied here):**

- **Crit 1 — no device data stream.** Does not acquire, process, or analyze a medical image, a signal from an in-vitro diagnostic, or a pattern/signal from a signal-acquisition system (monitor / EEG / processed-EEG depth-of-sedation index / continuous SpO₂ / lab-analyzer feed).
- **Crit 2 — displays/analyzes medical information.** Shows patient medical information and/or peer-reviewed studies and clinical practice guidelines.
- **Crit 3 — supports vs directs.** Provides options and complete information for the clinician to weigh — it does **not** emit a single push-button directive the clinician would rely on primarily.
- **Crit 4 — basis independently reviewable.** The clinician can independently review the basis (the inputs, the cited source, and the logic) so they do not rely primarily on the tool's output.

**Core enforcement principle.** Anywhere the tool emits **a number that drives an action** (a weighted score, an escalation threshold, a cut-point, a starting dose), that number implies a *calibration* the "explicitly unvalidated" framing disclaims. Such home-grown numbers must be **flattened, relabelled, or carry an explicit "pragmatic, not validated" note**, while validated, citable instruments (CAM-ICU, RASS, PADIS-anchored dosing) stay faithful and fully cited. Crit 3 and Crit 4 are where the outputs bite; Crit 1 and Crit 2 pass for every output **only because all inputs are manual clinician entry** — see the device-data-stream boundary at the end.

## Mapping table

| Output | Crit 1 — no device signal | Crit 2 — displays medical info | Crit 3 — supports vs directs | Crit 4 — basis reviewable | Verdict | Keep it qualifying |
|---|---|---|---|---|---|---|
| **Risk-factor tally** (0–16 count + tier box; flat +1 per factor; bands 0–3 / 4–6 / 7–10 / 11–16) | PASS — manual checkboxes | PASS — displays risk factors | PASS — flattened to an unweighted count, labelled "(heuristic)" / "not a validated score" beside the number; routes to E-PRE-DELIRIC / PRE-DELIRIC as the validated alternatives | PASS — declared pragmatic (tier P); no implied calibration remains | **Would-qualify** *(weights flattened and psychiatry trigger removed, 2026-06-27)* | Keep the "(heuristic)" label adjacent to the number; never re-introduce weights or a score-triggered consult |
| **Escalation suggestion** (geriatrics above a count of 6) | PASS | PASS | PASS — "Consider … (per local protocol)", explicitly "pragmatic, not validated"; the score-triggered psychiatry suggestion was removed | PASS — labelled pragmatic | **Would-qualify** | Keep "consider" phrasing; psychiatry remains feature-indicated, never count-triggered |
| **CAM-ICU result** (Positive / Negative / Unable to assess; two-step: no verdict until a RASS is documented; arousal gate at RASS −4/−5; standard 4-feature rule; Feature 3 = RASS ≠ 0 per the worksheet) | PASS — features and RASS are clinician-entered | PASS | PASS — explicitly "a screen, not a diagnosis — confirm clinically and exclude mimics," with a delirium-vs-mimics differential = options/complete info | PASS — validated instrument reproduced faithfully; `ely2001`, `camicu_worksheet`, `camicu_manual`, `icudelirium_monitoring` cited; algorithm fully visible; the 2026-07-01 revision removed the last deviation (descriptor-form Feature 3) and enforced the RASS-first order | **Would-qualify** *(reference example)* | Preserve the "screen not diagnosis" line, the mimic differential, and the RASS-first gating verbatim |
| **CAM-ICU inattention cut-points** (10-letter SAVEAHAART > 2 errors; Pictures ASE as the validated 10-item recognition task, same > 2/10 cut) | PASS | PASS | PASS | PASS — both modalities faithful to the worksheet (`ely2001`, `camicu_worksheet`); the earlier 5-card mislabel was corrected 2026-06-27 | **Would-qualify** | Keep error entry constrained 0–10 and both cut-points as published |
| **RASS readout + target-band zoning** (green at target / amber caution / red danger; default 0 to −2, configurable; deep −3 to −4 band indication-gated; −5 always red) | PASS — clinician picks the RASS value | PASS | PASS — supportive display; the deep band requires a documented indication; over-sedation always escalates | **Minor** — RASS validated (`sessler2002`, `ely2003`); the light target is PADIS-anchored; the amber/red zoning edges remain a labelled display aid, not a cited cut-point | **Would-qualify** | Keep the zoning labelled a display aid and the band configurable so a unit's own goal governs |
| **Motor-subtype classifier + figures** ("~23% of delirious cases, least common" etc.) | PASS | PASS | PASS — teaching classifier; drives no action/threshold | PASS — stated as the proportion of delirious cases (la Cour 2022), explicitly distinguished from population prevalence (Krewulak 2018); corrected 2026-06-27 | **Would-qualify** | Keep the denominator wording on every surface, including the printed SPA strip |
| **Treatment algorithm / agitation path** (entry RASS +2 to +4 → reversible causes → verbal de-escalation first → branch on CAM → Dr. DRE → intensify ABCDEF → pharm only on documented safety indication) — *time-pressured* | PASS — RASS/CAM clinician-entered | PASS | PASS — structured as supports: "non-pharm is the cornerstone; pharmacologic therapy is reserved for patient-safety concerns only"; antipsychotics "do not treat or shorten delirium (MIND-USA negative; PADIS 2025: unable to recommend for or against)"; presents de-escalation and reversible-cause branches as co-equal options | PASS — cited: `projectbeta` (de-escalation), `hayhurst2016` (Dr. DRE), `padis2018`/`padis2025`, `mindusa2018` (now attached to the categorical clause on screen as well as in the PDF) | **Would-qualify** | Keep the "safety indication only" gate and the antipsychotics-don't-treat framing verbatim. Ensure the decision tree keeps reading as branching *options*, not a funnel that terminates in a drug |
| **Dosing reference table** (haloperidol 0.25–0.5 mg q4–6h PRN; quetiapine 12.5–25 mg q12h; dexmedetomidine 0.2–0.7 mcg/kg/hr; lorazepam per CIWA; melatonin 0.5–3 mg) | PASS | PASS | PASS — framed "typical dose, lowest effective dose, cap per local protocol," "safety indication only," "not an order set" — a reference, not a directive | PASS — each range now labelled or cited by provenance: dexmedetomidine pinned to the Precedex label (`dex_label`) with guideline indications split by source (`padis2018`/`padis2025`) and the A2B caveat (`a2b2025`); melatonin labelled conventional sleep dosing not drawn from the cited trials, with the PADIS 2025 conditional recommendation cited; haloperidol/quetiapine labelled "conventional / expert starting references, not guideline-calibrated" | **Would-qualify** | Keep every dose either source-pinned or explicitly labelled conventional; keep haloperidol/quetiapine framed as short-term agitation control, never delirium treatment |
| **Thiamine prompt** (DELIRIUM(S) "U": parenteral thiamine before glucose; at-risk 100–300 mg IV/day; Wernicke regimens shown as divergent guidelines) | PASS | PASS | PASS — presents the EFNS and RCP regimens as divergent options, not one directive | PASS — cited `espen_icu`, `efns_wernicke`, `rcp_wernicke` (added 2026-06-27; the earlier uncited dose was this document's clearest Crit-4 gap) | **Would-qualify** | Keep the divergence framing — the honest signal is that guidelines disagree |
| **Deliriogenic-medication list** (103 agents, 11 categories; benzodiazepines/opioids/anticholinergics on by default, long tail opt-in; higher-risk flags on cited anchors only) | PASS | PASS | PASS — framed "a medication-review prompt, not a list of equally harmful drugs"; default-off long tail + per-item toggle = the clinician opts agents in | **Minor** — the higher-risk flags are anchored (benzodiazepines → PADIS; strong anticholinergics → Beers Table 7 / ACB scale, with glycopyrrolate unflagged because neither anchor lists it; meperidine → Beers); renal cautions are split to sources that carry them (`beers2023`, `dean2004_renal`); the AGS 2025 Alternatives companion is referenced. Individual agents remain category-cited rather than per-item-cited | **Would-qualify** | Keep the flags bound to their anchors — an agent absent from the anchor sources does not get a flag. Keep the "risk varies by agent, dose, route, organ function" disclaimer |
| **PDF escalation ladder** (SPA: Step 1 all patients · Step 2 CAM-positive · Step 3 persistent >1 shift → geriatrics/psychiatry · Step 4 safety risk only → haloperidol + QTc) | PASS | PASS | PASS — qualitative steps with "consider," not numeric triggers; Step 4 keeps "document safety indication first / lowest dose / none at discharge without psych Rx" and the IV off-label caution | PASS — mirrors the cited treatment stance | **Would-qualify** | Keep triggers qualitative (clinical state, not a tally number). Preserve the Step-4 safety-indication gate |
| **Completion percentages** (ABCDEF bundle %, DELIRIUM(S) reviewed %, treatment-steps count) | PASS | PASS | PASS — a documentation/progress indicator with no clinical action cut-point | PASS — bundle elements cited (`sccm_abcdef`, `pun2019`, `inouye1999`, `hshieh2015`) | **Would-qualify** | Never attach a numeric action threshold to the % (e.g., do not introduce "<80% complete → escalate") — that would convert a descriptive count into an uncited cut-point |
| **Pediatric screens** (CAPD ≥ 9; pCAM-ICU / psCAM-ICU features incl. both psCAM Feature-2 paths; SBS/RASS arousal gate at SBS −2/−3, RASS −4/−5) | PASS — all observations clinician-entered | PASS | PASS — same screen-not-diagnosis framing; results withheld until the required items are answered | PASS — validated instruments reproduced faithfully (Traube 2014, Smith 2011/2016, Curley 2006); the CAPD anchor-band mapping is disclosed as a score-neutral presentation choice (§6.6) | **Would-qualify** | Keep the arousal gate and both psCAM Feature-2 paths faithful to the source worksheets |
| **Bedside templates** (/templates/ printed sheets) | PASS — no patient data is entered or printed | PASS | PASS — a protocol-configuration designer; the sheets are checklists the clinician marks by hand | PASS — every clinical statement mirrors the interactive tool's cited content (§2.9 provenance rule); local additions are the unit's own and carry no citation by design | **Would-qualify** | Preserve the §2.9 "no new clinical values" provenance rule for any new template content |

## Cross-cutting: the "home-grown number" watch-list

Every self-generated number that could imply a calibration, with its current state:

1. **Risk-tally weights** — flattened to +1 (resolved 2026-06-27).
2. **Risk band cut-points** (0–3 / 4–6 / 7–10 / 11–16) — "(heuristic)" label kept adjacent to the number.
3. **Consult threshold** (geriatrics > 6) — labelled pragmatic; the psychiatry trigger was removed.
4. **Pictures ASE cut-point** — corrected to the validated 10-item > 2/10 task (resolved 2026-06-27).
5. **RASS amber/red zoning edges** — labelled a display aid; target cited to PADIS and configurable.
6. **Subtype figures** — labelled "proportion of delirious cases (la Cour 2022)" on every surface.
7. **Starting doses** — source-pinned (dexmedetomidine → label; weight-based pediatric doses → Capino 2020 / Phan 2008) or explicitly labelled conventional (haloperidol, quetiapine, melatonin).
8. **Thiamine regimens** — cited (ESPEN / EFNS / RCP) with the divergence stated (resolved 2026-06-27).

Validated, citable instruments stay faithful and fully cited and need no flattening: **CAM-ICU** (two-step, four features, both ASE modalities), **RASS** scale + PADIS target, the **pediatric instruments** (CAPD, pCAM-ICU, psCAM-ICU, SBS), and **PADIS-anchored** drug stances (dexmedetomidine niche; antipsychotics for agitation/symptom control only, never as delirium treatment per the MIND-USA-negative result).

## Device-data-stream boundary (Crit 1)

Every output above passes Crit 1 **only because all inputs are typed/checked by the clinician** — RASS is picked, CAM features are answered, inattention errors are counted by the bedside assessor, risk factors are checkboxes. Nothing is computed from a device waveform.

The tool stays non-device **only while it does not ingest a device signal.** Crit 1 flips to FAIL — and the non-device status is lost regardless of how well criteria 2–4 are met — the moment any output is derived from:

- a bedside monitor feed (continuous SpO₂, HR, BP),
- a processed-EEG depth-of-sedation index (e.g., a BIS/SedLine-type value) used to set or auto-fill RASS or the sedation zoning,
- a raw or processed EEG used to call the "Ictal/Seizure" domain or a delirium index,
- an in-vitro-diagnostic / lab-analyzer feed used to drive the tally, the metabolic domain, or any threshold.

**Keep all inputs manual.** If EHR integration is ever added, restrict it to *displaying/transcribing* values the clinician confirms — never analyzing a device signal to generate the CAM result, the RASS zone, the risk tally, or any escalation. Auto-pulling a monitor or EEG value to compute an output is the single change that would move this tool from non-device CDS into device territory. (The Feature-3 hint derived from the *clinician-entered* RASS stays on the manual side of this line — it re-displays a typed value; it analyzes no signal.)
