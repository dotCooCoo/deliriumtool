# Non-Device CDS design rubric — output-by-output mapping

**Purpose.** A design check, not legal advice. Each clinical *output* of the tool is held against the four FDA Non-Device Clinical Decision Support criteria (21st Century Cures Act §520(o)(1)(E), as structured in the Jan-2026 CDS final guidance). The goal is that every output is **built so it would qualify** as non-device CDS — even though an internal, non-distributed reference aid is outside FDA premarket reach.

**The four criteria (as applied here):**

- **Crit 1 — no device data stream.** Does not acquire, process, or analyze a medical image, a signal from an in-vitro diagnostic, or a pattern/signal from a signal-acquisition system (monitor / EEG / processed-EEG depth-of-sedation index / continuous SpO₂ / lab-analyzer feed).
- **Crit 2 — displays/analyzes medical information.** Shows patient medical information and/or peer-reviewed studies and clinical practice guidelines.
- **Crit 3 — supports vs directs.** Provides options and complete information for the clinician to weigh — it does **not** emit a single push-button directive the clinician would rely on primarily.
- **Crit 4 — basis independently reviewable.** The clinician can independently review the basis (the inputs, the cited source, and the logic) so they do not rely primarily on the tool's output.

**Core enforcement principle.** Anywhere the tool emits **a number that drives an action** (a weighted score, an escalation threshold, a cut-point, a starting dose), that number implies a *calibration* the "explicitly unvalidated" framing disclaims. Such home-grown numbers must be **flattened, relabelled, or carry an explicit "pragmatic, not validated" note**, while validated, citable instruments (CAM-ICU, RASS, PADIS-anchored dosing) stay faithful and fully cited. Crit 3 and Crit 4 are where the outputs bite; Crit 1 and Crit 2 pass for every output **only because all inputs are manual clinician entry** — see the device-data-stream boundary at the end.

## Mapping table

| Output | Crit 1 — no device signal | Crit 2 — displays medical info | Crit 3 — supports vs directs | Crit 4 — basis reviewable | Verdict | Fix to make it qualify |
|---|---|---|---|---|---|---|
| **Risk-factor tally** (0–17 number + colored tier box; +2 dementia / +1 each other; bands 0–3 / 4–6 / 7–10 / 11–17) | PASS — manual checkboxes | PASS — displays risk factors | **AT-RISK** — a single weighted *number* in a color-tiered box reads as a calibrated risk score; the lone +2 dementia weight implies a validated weighting it does not have | **AT-RISK** — per-factor weights and band cut-points map to **no published score** (self-described "pragmatic"); the +2 and the 0–3/4–6/7–10/11+ boundaries are not reviewable against any citation | **At-risk** | Flatten weights to all +1 (or render as a non-numeric checklist), keep the "(heuristic)" / "not a validated score" labels next to the number, keep the E-PRE-DELIRIC / PRE-DELIRIC routing as the validated alternative, and de-emphasize the risk-score color styling so it does not mimic a calibrated instrument |
| **Escalation suggestions** (geriatrics when score > 6; psychiatry + "increase CAM frequency" when score > 10) | PASS | PASS | **AT-RISK** — a home-grown numeric trigger (>6, >10) firing a consult recommendation is a number driving an action; "Consider … (per local protocol)" softens but does not remove the implied cut-point | **AT-RISK** — the >6 / >10 thresholds carry no citation | **At-risk** | Detach the consult suggestion from the numeric threshold: present "consider geriatrics / psychiatry input per local protocol" as a standing option independent of the tally, or label the trigger "pragmatic, not validated." Keep the "Consider" (option, not order) phrasing already in place |
| **CAM-ICU result** (Positive / Negative / Unable to assess; arousal gate at RASS −4/−5; standard 4-feature rule) | PASS — features and RASS are clinician-entered | PASS | PASS — explicitly "a screen, not a diagnosis — confirm clinically and exclude mimics," with a delirium-vs-mimics differential = options/complete info | PASS — validated instrument; `ely2001`, `camicu_worksheet`, `icudelirium_monitoring` cited; algorithm fully visible | **Would-qualify** *(reference example)* | None needed. Preserve the "screen not diagnosis" line and the mimic differential verbatim — this is the model of a supports-not-directs output |
| **CAM-ICU inattention cut-point — 10-letter SAVEAHAART (>2 errors)** | PASS | PASS | PASS | PASS — faithful to the validated CAM-ICU letters task (`ely2001`, worksheet) | **Would-qualify** | None — keep error entry constrained 0–10 and the >2 cut-point as published |
| **CAM-ICU inattention — 5-card "Pictures ASE" reusing the same >2-error cut-point** | PASS | PASS | **AT-RISK** — applies a cut-point to a *different modality* where it is not validated | **FAIL** — `>2/5` (40% error bar) is **not** equivalent to the validated 10-item ASE `>2/10` (20% bar); presenting them as the same implies a calibration the cited worksheet does not support | **At-risk** *(genuine correctness defect)* | Use the validated picture-recognition scoring for the Pictures ASE (the published method scores recognition over the full 10-picture set, not a 5-card >2 rule), **or** relabel the 5-card variant as an un-scored teaching aid, **or** cite the specific Pictures-ASE source and its own cut-point. Do not present >2/5 as equivalent to the letters cut-point |
| **RASS readout + target-band zoning** (green at target / amber caution / red danger; default 0 to −2, configurable; over-sedation escalates) | PASS — clinician picks the RASS value | PASS | PASS — supportive display; underlying scale is validated and the asymmetric tone (agitation always flagged, over-sedation escalates) is clinical framing, not a directive | **Minor** — RASS validated (`sessler2002`, `ely2003`); the 0 to −2 target is PADIS light-sedation and configurable; only the amber/red *zoning thresholds* (amber +1/−3, red +2/−4) are an uncited interpretation | **Would-qualify** | Cite the light-sedation target to PADIS; label the amber/red zoning as a "display aid — target per local protocol"; keep the band configurable so a unit's own goal governs |
| **Motor-subtype classifier + figures** ("~23%, least common" hyper; "~50%, most common, often missed" hypo; mixed "longest duration/LOS") | PASS | PASS | PASS — teaching classifier; drives no action/threshold | **AT-RISK** — figures are presented as "prevalence" but are the **proportion of delirious cases** (la Cour 2022: hypo 50.3% / mixed 27.7% / hyper 22.7%), **not** population prevalence (Krewulak 2018: ~31% overall). The label misstates what the number means, so the basis is not accurately reviewable | **At-risk** | Relabel as "proportion of delirious cases (la Cour 2022)" and use the exact figures (hypo 50.3 / mixed 27.7 / hyper 22.7), explicitly distinguished from population incidence (Krewulak). Or drop the percentages and keep the qualitative "most common / least common / often missed" |
| **Treatment algorithm / agitation path** (entry RASS +2 to +4 → reversible causes → verbal de-escalation first → branch on CAM → Dr. DRE → intensify ABCDEF → pharm only on documented safety indication) — *time-pressured* | PASS — RASS/CAM clinician-entered | PASS | PASS — structured as supports: "non-pharm is the cornerstone; pharmacologic therapy is reserved for patient-safety concerns only"; antipsychotics "do not treat or shorten delirium (PADIS: no recommendation for or against)"; presents de-escalation and reversible-cause branches as co-equal options | PASS — cited: `projectbeta` (de-escalation), `hayhurst2016` (Dr. DRE), `padis2018`/`padis2025`, `mindusa2018` | **Would-qualify** | Keep the "safety indication only" gate and the antipsychotics-don't-treat framing verbatim (MIND-USA-negative). Ensure the decision tree keeps reading as branching *options*, not a funnel that terminates in a drug. Frame haloperidol/quetiapine as short-term **agitation / symptom control**, dexmedetomidine to its ventilated-agitation niche — never as treatment for delirium |
| **Dosing reference table** (haloperidol 0.25–0.5 mg q4–6h PRN; quetiapine 12.5–25 mg q12h; dexmedetomidine 0.2–0.7 mcg/kg/hr; lorazepam per CIWA; melatonin 0.5–3 mg) | PASS | PASS | **Borderline → PASS** — a specific dose is a number that drives an action, but it is framed "typical dose, lowest effective dose, cap per local protocol," "safety indication only," "not an order set" — keeps it a reference, not a directive | **Minor** — agent-level evidence cited (dexmed → `padis2025`/`mends2`; haloperidol → `haldol_label`/`mindusa2018`; melatonin → `promedic2022`); the *exact* starting numbers are conventional, not pinned to a specific guideline figure | **Would-qualify** | Keep each dose as a cited reference range with "starting reference, not a dose recommendation — verify against formulary/policy." Attribute the specific ranges to a source (label / PADIS / standard reference) or mark "conventional starting range, not guideline-calibrated." Keep haloperidol/quetiapine labelled for short-term agitation control, not delirium treatment |
| **Thiamine prompt** (DELIRIUM(S) "U": parenteral thiamine before glucose, 100–300 mg IV; **500 mg IV TID if Wernicke suspected**) | PASS | PASS | **AT-RISK** — a specific dose driving an action | **FAIL** — **no thiamine citation exists in the registry**; the dose's basis is not independently reviewable (clearest Crit-4 gap in the tool) | **At-risk** | Add a citation (ESPEN nutrition guidance; RCP Wernicke guidance) and note the high dose is a short ~3–5 day course; **or**, until a source is added, soften to "parenteral thiamine before glucose per local protocol" without the specific mg |
| **Deliriogenic-medication list** (103 agents, 11 categories; long tail opt-in — 34 on by default, 69 off; 25 agents flagged higher-risk; no per-item citation) | PASS | PASS | PASS — framed "a medication-review prompt, not a list of equally harmful drugs"; default-off long tail + per-item toggle = the clinician opts agents in | **AT-RISK** — no per-item citation; only Beers 2023 / PADIS at the category level, and the higher-risk flags are not individually cited. "Why is this specific agent here / how risky" is still only category-reviewable | **At-risk** | Anchor each agent (or at least each category) to a source class — Beers anticholinergic burden, PADIS for benzodiazepines, named neurotoxicity references for cefepime/meperidine. Keep the "risk varies by agent, dose, route, organ function" disclaimer. The higher-risk flags should themselves carry a cited basis. |
| **PDF escalation ladder** (SPA: Step 1 all patients · Step 2 CAM-positive · Step 3 persistent >1 shift → geriatrics/psychiatry · Step 4 safety risk only → haloperidol + QTc) | PASS | PASS | PASS — qualitative steps with "consider," not numeric triggers; Step 4 keeps "document safety indication first / lowest dose / none at discharge without psych Rx" | PASS — mirrors the cited treatment stance | **Would-qualify** | Keep triggers qualitative (clinical state, not a tally number). Preserve the Step-4 safety-indication gate |
| **Completion percentages** (ABCDEF bundle %, DELIRIUM(S) reviewed %, treatment-steps count) | PASS | PASS | PASS — a documentation/progress indicator with no clinical action cut-point | PASS — bundle elements cited (`sccm_abcdef`, `pun2019`, `inouye1999`, `hshieh2015`) | **Would-qualify** | None. Never attach a numeric action threshold to the % (e.g., do not introduce "<80% complete → escalate") — that would convert a descriptive count into an uncited cut-point |

## Cross-cutting: the "home-grown number" list

These are every output where a self-generated number currently implies a calibration. Each must be flattened, relabelled, or flagged "pragmatic, not validated":

1. **Risk-tally weights** (+2 dementia / +1 each) → flatten to +1 or relabel non-numeric.
2. **Risk band cut-points** (0–3 / 4–6 / 7–10 / 11–17) → already "(heuristic)"; keep that label adjacent to the number, not only in a footnote.
3. **Consult thresholds** (>6 geriatrics, >10 psychiatry) → detach from the number or label pragmatic.
4. **5-card Pictures ASE >2 cut-point** → correctness fix (validated scoring or relabel as teaching aid) — this is the one genuine accuracy defect, not just a labeling nuance.
5. **RASS amber/red zoning thresholds** → label "display aid," cite the 0 to −2 target to PADIS.
6. **Subtype "prevalence" figures** → relabel "proportion of delirious cases (la Cour 2022)," exact figures, distinguished from population prevalence.
7. **Starting doses** → keep as cited reference ranges with "not a dose recommendation."
8. **Thiamine dose** → add ESPEN/RCP citation (currently uncited) + "short ~3–5 day course."

Validated, citable instruments stay faithful and fully cited and need no flattening: **CAM-ICU** (10-letter task), **RASS** scale + PADIS target, **PADIS-anchored** drug stances (dexmedetomidine niche; antipsychotics for agitation/symptom control only, never as delirium treatment per the MIND-USA-negative result).

## Device-data-stream boundary (Crit 1)

Every output above passes Crit 1 **only because all inputs are typed/checked by the clinician** — RASS is picked, CAM features are answered, inattention errors are counted by the bedside assessor, risk factors are checkboxes. Nothing is computed from a device waveform.

The tool stays non-device **only while it does not ingest a device signal.** Crit 1 flips to FAIL — and the non-device status is lost regardless of how well criteria 2–4 are met — the moment any output is derived from:

- a bedside monitor feed (continuous SpO₂, HR, BP),
- a processed-EEG depth-of-sedation index (e.g., a BIS/SedLine-type value) used to set or auto-fill RASS or the sedation zoning,
- a raw or processed EEG used to call the "Ictal/Seizure" domain or a delirium index,
- an in-vitro-diagnostic / lab-analyzer feed used to drive the tally, the metabolic domain, or any threshold.

**Keep all inputs manual.** If EHR integration is ever added, restrict it to *displaying/transcribing* values the clinician confirms — never analyzing a device signal to generate the CAM result, the RASS zone, the risk tally, or any escalation. Auto-pulling a monitor or EEG value to compute an output is the single change that would move this tool from non-device CDS into device territory.