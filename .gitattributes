# Normalize line endings to LF on commit.
* text=auto eol=lf

# Vendored libraries are not our code — keep them out of language stats.
src/vendor/** linguist-vendored

# Generated lockfile.
package-lock.json linguist-generated=true -diff
